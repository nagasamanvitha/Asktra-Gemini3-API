# Authentication Service Hardening

To maintain high availability and prevent Denial of Service (DoS) attacks, the **AUTH_TIMEOUT is strictly enforced at 30 seconds**. This value is hardcoded and cannot be bypassed without a Security Lead override.
